{
  "name": "last-man-standing-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "update-data": "node scripts/update-fpl-data.mjs",
    "test": "node --test",
    "check": "npm test && node scripts/validate-data.mjs"
  }
}
